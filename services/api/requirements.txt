# Vercel's Python runtime installs from this file, not from pyproject.toml.
# Kept in step with the [project.dependencies] list in pyproject.toml.
fastapi>=0.115,<1
sqlalchemy>=2.0,<2.1
alembic>=1.13,<2
psycopg[binary]>=3.2,<4
firebase-admin>=6,<8
python-multipart>=0.0.9,<1
