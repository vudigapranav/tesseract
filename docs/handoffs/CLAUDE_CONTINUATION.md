# Claude continuation prompt — paused 2026-09-07

Resume only when the user asks. Codex stopped implementation at the user's explicit request and left uncommitted work. Complete Tesseract's non-game Flutter patient/caregiver frontend and Android application integration, preserving the production-quality requirement. Skeleton/demo-only work is not completion. Do not claim production readiness while external configuration or required validation is missing.

**Work in `/Users/pranav07vudiga/.codex/worktrees/4fa8/pranav07vudiga/tesseract`**, branch `codex/patient-caregiver-integration`, based on backend commit `00f5cc9`. This is a nested isolated clone. The parent is an unrelated UserProfileApp repo; the original `/Users/pranav07vudiga/Desktop/Projects/Hackathon/SIH` is untouched and does not contain these changes. Inspect status and preserve all uncommitted work. Do not push or touch the unrelated parent repo.

First read Brain.md and PS003_MOBILE_CODE_STATUS.md, especially the appended **Codex non-game integration — STOPPED BY USER** record. It inventories every change, actual evidence, unfinished wiring and known risks. Read AGENTS.md, PS003_MASTER_CONTEXT.md, docs/PS003_BUILD_HANDBOOK.md, docs/PS003_ROADMAP.md, docs/contracts/PS003_API_CONTRACT_V1.md and docs/handoffs/SHANKS_API_REVIEW.md. Several earlier combined reads were truncated: read fully as needed. Inspect all diffs/new files before proceeding.

Reference image: `/var/folders/4_/rgvg3fz13bj3f8wmgvvwklmr0000gn/T/codex-clipboard-b477698e-8ccb-42f6-a215-f0a05380e5ac.png`. Codex inspected it. Inspect it yourself; if unavailable, say so. Direction: warm cream/peach gradients, decorative coral, white rounded cards, black pill buttons, strong headings, generous spacing. Tesseract/activity illustration, no medical marketplace features or baked-in phone frame. Caregiver dashboard rich, patient screens simpler with large labelled actions, permanent Help/Break and no developer event controls. Respect large text, contrast, TalkBack, reduced motion, Android safe areas and small screens.

Ownership: Shanks shared architecture/navigation/host/integration; Maharshitha design assets/components via explicit handoff notes; Pranav backend/API/analytics. Nobody was contacted. Do not redesign games, import friends' repositories, modify the submitted PPT or independently change backend contracts. Ruthika's repositories are later separate work. Both Route Quest and Marble Maze stay registered. Marble Maze remains gyroscope-first with accessible touch fallback.

Start by reviewing and fixing the blocking issues listed in the stop record: identity-partitioned local data; real patient selection/creation; durable write/recovery correctness; authenticated outbox retries; approved config/version/content propagation; manual local handover changes versus backend approval; stale postponed notification cancellation; notification occurrence/tap/time-zone plumbing; save-error handling and live accessibility preferences. Do not mistake written scaffolding for accepted behavior. Shared lifecycle pause/resume forwarding fix and its test are written but untested. The latest reminder/platform code has not even been analyzed.

Complete P1–P9/C1–C7: verified sign-in/access states; saved basics/Know Me/preferences; actual history/reminders/pending recommendation dashboard with accept/modify/reject; protected handover/return and supported accessible input; patient home/selection/approved personalized activity/useful instructions/play/finished/rest/history; reminder create/edit/enable/postpone/acknowledge; settings/sign-out/truthful sync. Add repository/service abstractions and durable SQLite, stable ordered outbox IDs, retry/dedup/restart recovery. Preserve approved settings offline. Adapt elapsedMs at host boundary; do not fabricate actual input mode or missing efficiency metrics. Fix shared lifecycle defects within ownership, report game dependencies instead of changing game packages.

Use the real backend at 00f5cc9 and its draft contract. P1–P7 and §14 are reviewed in notes but not team-approved/frozen. User explicitly chose host elapsed_ms mapping. Propose batches <=500 and existing three-call sequence. Actual Maze input mode cannot currently be observed via game callbacks. Profile-basics update endpoint was absent on inspection; do not invent one. Preserve conflicts for review, not silent overwrite. Do not change backend contract without Pranav.

Public Firebase project/client configuration, sign-in provider and HTTPS backend URL were requested but unanswered when stopped. Current adapter supports email/password as an optional implementation, not a confirmed provider choice. Validate real Firebase against actual project before claiming it works. Keep secrets out of mobile/chat. Synthetic preview is explicitly gated by TESSERACT_ALLOW_PREVIEW; never fallback from real auth failures. Doctor D1–D8 platform also unanswered: implement frontend only after confirmed; if web, bounded separate portal package, and do not count mobile screens as fulfillment.

Verification: format/analyze first; add meaningful behavioral tests for lifecycle event sink, SQLite reopen, interrupted and terminal recovery, duplicate/lost-response/partial rejection outbox replay, identity isolation, protected handover/return, reminder edits/permission/time-zone/restart and navigation/accessibility. Verify caregiver setup → protected handover → session → durable history, restart, offline/reconnect and reminders. Capture settled screenshots and inspect them. Build a current Android APK. Run device inventory; mark physical checks NOT TESTED if no phone. Historical backend tests/builds are not current evidence. Codex ran only dependency resolution and two interim analyses (first clean; second two style infos); no tests/APK/screenshots/device QA this pass.

Update Brain.md and PS003_MOBILE_CODE_STATUS.md after meaningful milestones with actual files/checks/evidence and limitations. Keep commits scoped; no push, paid deployment or unrelated modifications. Deliver completed app/design system, API notes, screenshots, current build/tests and explicit remaining blockers. Do not stop at a plan while safe authorized implementation remains.
