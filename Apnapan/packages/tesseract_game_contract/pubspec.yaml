name: tesseract_game_contract
description: >
  Shared contract between the Tesseract host app and its games. A game is a
  self-contained widget that receives everything (config, content, strings)
  and reports everything (events, result) through callbacks. No dependency
  beyond Flutter itself.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: '>=3.19.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
